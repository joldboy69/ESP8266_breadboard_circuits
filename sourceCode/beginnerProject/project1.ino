int a = 13; // sets variable "a" equal to pin 13 on our board

void setup() { // setup code to run only once
  
  pinMode(a, OUTPUT); // sets the board to utilize pin 13 (which we set as "a" above) as the output

void loop() { // creates a loop
  

  digitalWrite(a, HIGH); // turns the pin charge on 

  

}
